# 迭代原始需求输入

> 所有权：目录入口说明由工作流提供；其中除本 README 外的文件属于实例项目的用户原始输入，必须原样保留。

本目录保存用户提供的迭代原始需求，保留文件格式、文件名与原文。运行 `python .workflow/workflow.py route-requirement` 确定当前输入对应的目标版本。

除本说明文件外，目录中的原始需求归用户所有且只读：Agent 不得修改、重命名或删除。`route-requirement` 返回目标版本；归一化产物必须记录所读取的原始文件路径和目标版本。
